<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sanju.googlesignin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".secondOne.ProfileActivity"></activity>
        <activity android:name=".secondOne.LoginActivity" />
        <activity android:name=".secondOne.RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".firstOne.LogoutActivity" />
        <activity android:name=".firstOne.GoogleActivity" />
        <activity android:name=".firstOne.SecondActivity" />
        <activity android:name=".firstOne.MainActivity" />
    </application>

</manifest>